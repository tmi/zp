[project]
name = "suite"
version = "0.1.0"
description = "Benchmark suite driver and analysis scripts"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.1.0",
    "ty==0.0.15",
]
