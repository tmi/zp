system prompt should be a file, so that I can change it easily

vim mode

history of the chat window, ie, up arrow. Across sessions

favourite prompts or templates

logging of the mcp servers -- the stderr of the process should go into our log file, or dedicated log file
